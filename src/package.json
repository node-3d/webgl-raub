{
	"name": "build",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "node-gyp rebuild && node -e \"require('addon-tools-raub/cpbin')('webgl')\""
	},
	"dependencies": {
		"addon-tools-raub": "5.0.0",
		"deps-opengl-raub": "3.0.0"
	}
}
